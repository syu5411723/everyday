import styled from "styled-components"
const wrapper = styled.div`

`

type Props = {
    
}

export const HomeContent = () => {
    return (
        <div>
            
            
            
        </div>
    )
}
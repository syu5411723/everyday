import styled from "styled-components"
const wrapper = styled.div`

`

type Props = {
    text: string
}

export const HomeContent = () => {
    return (
        <div>
            
            
            
        </div>
    )
}